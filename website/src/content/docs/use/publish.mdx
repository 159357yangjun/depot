---
title: 文件、URL 与剪贴板发布
---

一次发布有三种入口，但最后都进入同一套 Workflow：

- **本地文件**：拖拽或文件选择，多张图片受控并发上传。
- **图片 URL**：每行一个 URL，桌面端先下载，再执行 Resize / WebP / Rename / 多云发布。
- **剪贴板**：复制截图或位图后直接发布，不需要先保存成文件。

建议先创建默认方案。以后打开上传窗口时，Publisher 会自动选中默认 Workflow。
